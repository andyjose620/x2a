include _config
include _functions
include _mixins

block variables
- main();

doctype html
html( lang='en', class= htmlClass )
	head
		block title
			title= pageName

		meta( charset='utf-8' )
		meta( name='viewport', content='width=device-width, height=device-height, initial-scale=1.0' )
		meta( http-equiv='X-UA-Compatible', content='IE=edge' )

		link( rel='icon', href='images/favicon.ico', type='image/x-icon' )
		link( rel='stylesheet', type='text/css', href= fonts )
		link( rel='stylesheet', href='css/bootstrap.css' )
		link( rel='stylesheet', href='css/fonts.css' )
		link( rel='stylesheet', href='css/style.css' )

		style .ie-panel{display: none;background: #212121;padding: 10px 0;box-shadow: 3px 3px 5px 0 rgba(0,0,0,.3);clear: both;text-align:center;position: relative;z-index: 1;} html.ie-10 .ie-panel, html.lt-ie-10 .ie-panel {display: block;}

	body
		.ie-panel
			a( href='http://windows.microsoft.com/en-US/internet-explorer/' )
				img( src='images/ie8-panel/warning_bar_0000_us.jpg', height=42, width=820, alt='You are using an outdated browser. For a faster, safer browsing experience, upgrade for free today.' )

		.preloader
			.preloader-body
				.cssload-container: .cssload-speeding-wheel

		.page
			block page

		#exampleModal.modal.modal-1(tabindex='-1', role='dialog')
			.modal-dialog(role='document')
				.modal-content
					.modal-header
						button.close(type='button', data-dismiss='modal', aria-label='Close')
							span(aria-hidden='true') ×
					.modal-body
						h4.modal-title Get in Touch
						// RD Mailform
						form.rd-mailform(data-form-output="form-output-global", data-form-type='contact', method='post', action='bat/rd-mailform.php')
							.row.align-items-md-end.row-20
								.col-md-6
									.form-wrap
										input#booking-name.form-input(type="text" name="first-name" data-constraints='@Required')
										label.form-label(for="booking-name") Name

								.col-md-6
									.form-wrap
										input#booking-phone.form-input(type="text" name="phone" data-constraints='@Numeric @Required')
										label.form-label(for="booking-phone") Phone

								.col-md-6
									.form-wrap
										input#booking-date.form-input( type="text" name="date" data-constraints="@Required" data-time-picker="date" )
										label.form-label(for="booking-date") DD/MM/YY

								.col-md-6
									.form-wrap
										input#booking-email.form-input(type="email" name="email" data-constraints="@Email @Required")
										label.form-label(for="booking-email") E-mail

								.col-md-6
									button(type="submit").button.button-block.button-primary Send Message

		#form-output-global.snackbars

		script( src='js/core.min.js' )
		script( src='js/script.js' )

		if( pageName === 'Home' )
			//coded by Ragnar
